plugins {
    id 'geoclient.library-conventions'
    id 'geoclient.test-conventions'
}

dependencies {
    implementation(libs.jackson.dataformat.xml)
    // Needed to put the geoclient.xml file on the classpath for testing
    testImplementation(testFixtures(project(':geoclient-core')))
}
